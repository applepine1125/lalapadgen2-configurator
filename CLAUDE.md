# Lala2Conf

LalaPad Gen2 のトラックパッドとキー設定を調整する macOS アプリと、その中身のページ。
ファームウェア側は [zmk-config-LalaPadGen2](https://github.com/applepine1125/zmk-config-LalaPadGen2)。

| 場所 | 中身 |
| --- | --- |
| `web/` | 画面とロジック。依存なしの HTML + JavaScript。IIFE で `root.TpXxx` と `module.exports` の両対応 |
| `mac/` | macOS アプリ。Swift + WKWebView。swiftc だけでビルドする(パッケージマネージャなし) |

## よく使うコマンド

```sh
node --test web/*.test.js        # ページのテスト(ディレクトリ指定は CI の Node で失敗する)
bash mac/build.sh --run          # ビルドして起動
bash mac/build.sh --install      # /Applications/Lala2Conf.app に入れる
bash mac/release.sh              # 下書きリリースに署名して公開する(下記)
```

実機がなくても `web/index.html?fakeNative=1` で画面を確認できる(`&fakeLive=1` でダミーのフレームが流れる)。

## リリース手順(手元でのコマンド実行が要る)

`main` に push すると CI がビルドして **下書きの**リリースを作る。**下書きのままではアプリから見えない**(未認証の GitHub API には出ないため)。公開するには手元で署名する。

```sh
bash mac/release.sh          # 最新の下書きを署名して公開する
bash mac/release.sh --wait   # CI のビルドを待ってから署名して公開する
```

つまり **push しただけではリリースは配られない**。push のあとにこのコマンドを実行するところまでがリリース作業。忘れていてもアプリが新しいビルドに気づかないだけで、壊れはしない。

## 自動更新の署名(この設計を壊さないこと)

アプリは GitHub Releases の zip と `.sig` を落とし、`mac/Sources/Updater.swift` に埋め込んだ公開鍵(ed25519 / CryptoKit)で検証してから、自分のバンドルを置き換えて再起動する。署名が無い・合わなければ更新しない。

自動更新は「自分自身を丸ごと置き換えて再起動する」ので、検証が無いと GitHub のアカウントやリポジトリを取られた時点でこの Mac での任意コード実行に直結する。Gatekeeper はこの経路を見ていない(アプリ自身が落としたファイルには `com.apple.quarantine` が付かず、評価が発火しない)。だから自前で検証している。

**守るべき不変条件**:

- **秘密鍵(`~/.config/lala2conf/signing.key`)を CI に置かない。** 置いた時点で、リポジトリを取れた相手がそのワークフローで署名できてしまい、検証の意味が完全に無くなる
- **人の確認を挟まずに署名しない。** 「下書きを見つけたら自動で署名する」仕組み(launchd、CI、bot)を入れると、攻撃者が push したビルドも同じ仕組みが署名する。この対策の価値は「鍵を持つ人間が、自分のビルドだと確認してから署名する」点にしかない
- **セルフホストランナーを使わない。** 攻撃者の push が自分の Mac で動くことになり、対策前より悪化する
- **公開鍵を作り直さない。** 古い公開鍵が入ったアプリは更新できなくなる(手元でビルドし直して入れ替えが必要)

自動化してよいのは「人の 1 アクションを軽くする」ところまで(タグの調べ直しを無くす、CI の完了を待つ、内容を表示して確認を取る)。

## 書き方の約束

- コメント・ドキュメント・コミットメッセージ・テスト名は日本語
- テスト名は「<given>のとき、<when>すると<then>になる」
- 自明なコメントは書かない。書くのは「なぜそうしたか」
- ページ側のモジュールは依存を増やさない(ビルド手順を持たないのが前提)
